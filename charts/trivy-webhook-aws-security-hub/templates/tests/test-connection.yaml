apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trivy-webhook-aws-security-hub.fullname" . }}-test-connection"
  labels:
    {{- include "trivy-webhook-aws-security-hub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trivy-webhook-aws-security-hub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
